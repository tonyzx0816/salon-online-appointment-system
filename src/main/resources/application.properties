spring.application.name=starter-demo

# Base URL of the mocked external notification vendor (same app in this demo).
app.external.notification.base-url=http://localhost:8080

# Expose basic actuator observability endpoints for system management.
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
