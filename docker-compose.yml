services:

  db:
    image: postgres:15.2-alpine
    environment:
      POSTGRES_DB: salon
      POSTGRES_PASSWORD: secret
    volumes:
      - salon-pg-local:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  salon-pg-local:
